import { lightGreen } from "@suid/material/colors";
import { createTheme } from "@suid/material/styles";


export const MainTheme = createTheme({
  palette: {
    primary: {
      main: lightGreen[800],
    },
    secondary: {
      main: "#BAF88C",
    },
  },
});